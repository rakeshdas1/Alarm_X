<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="@string/intro"
        android:id="@+id/introTxt"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="69dp" />

    <Spinner
        android:layout_width="20dp"
        android:layout_height="wrap_content"
        android:id="@+id/hrSpn"
        android:layout_marginTop="73dp"
        android:layout_toStartOf="@+id/introTxt"
        android:layout_below="@+id/introTxt"
        android:layout_alignParentStart="true"
        android:entries="@array/hour"
        android:focusable="true" />

    <Spinner
        android:layout_width="20dp"
        android:layout_height="wrap_content"
        android:id="@+id/minSpn"
        android:entries="@array/minutes"
        android:layout_alignTop="@+id/hrSpn"
        android:layout_toEndOf="@+id/hrSpn"
        android:layout_marginLeft="46dp"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/set"
        android:id="@+id/setBtn"
        android:layout_below="@+id/hrSpn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="58dp" />
</RelativeLayout>
